---
/**
 * Footer
 * Site footer with animated gradient wave, social links, and copyright.
 *
 * @depends config.site.name, SocialLinks
 * @styles Scoped SCSS — footer layout, wave animation + responsive
 */
import SocialLinks from './SocialLinks.astro';
import LighthouseScores from './LighthouseScores.astro';
import { config } from '@config/index';
import type { TranslationMap } from '@i18n/types';

interface Props {
  translations: TranslationMap;
}

const { translations } = Astro.props;
const year = new Date().getFullYear();
const builtWithText = translations.footer.builtWith.split('{heart}');
---

<!-- ══════ FOOTER ══════ -->
<footer>
  <div class="footer-wave"></div>
  <LighthouseScores />
  <div class="footer-socials">
    <SocialLinks />
  </div>
  <p class="footer-copy">&copy; {year} {config.site.name}. {builtWithText[0]}<span class="heart">&hearts;</span>{builtWithText[1]}</p>
</footer>

<style lang="scss">
  @use '../styles/mixins' as *;

  footer {
    position: relative;
    z-index: 1;
    text-align: center;
    padding: 4rem 2rem 2rem;
    border-top: 1px solid var(--glass-border);

    @include respond-to(mobile) { padding: 3rem 1.25rem 1.5rem; }
    @include respond-to(small) { padding: 2rem 1rem 1.25rem; }
  }

  .footer-wave {
    position: absolute;
    top: -2px; left: 0; right: 0;
    height: 4px;
    background: linear-gradient(90deg, var(--accent), var(--accent-blue), var(--accent-tertiary), var(--accent));
    background-size: 300% 100%;
    animation: waveGradient 6s linear infinite;
    will-change: background-position;
  }

  @keyframes waveGradient {
    0% { background-position: 0% 50%; }
    100% { background-position: 300% 50%; }
  }

  .footer-socials {
    display: flex;
    gap: 1rem;
    justify-content: center;
    margin-bottom: 2rem;

    @include respond-to(mobile) { gap: 0.75rem; }
    @include respond-to(small) { margin-bottom: 1.25rem; gap: 0.5rem; }
  }

  .footer-copy {
    font-size: 0.85rem;
    color: var(--text-dim);

    .heart { color: #ff5f57; }

    @include respond-to(small) { font-size: 0.75rem; }
  }
</style>
